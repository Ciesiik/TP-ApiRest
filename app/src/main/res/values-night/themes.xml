<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TPApiRest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/amarillo</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="editTextStyle">@style/MiEditText</item>
        <item name="checkboxStyle">@style/MiCheckBox</item>
        <item name="android:actionOverflowButtonStyle">@style/MiOverflowButton</item>
    </style>

    <style name="Theme.TPApiRest" parent="Base.Theme.TPApiRest" />
</resources>